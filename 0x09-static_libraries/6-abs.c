#include "main.h"
/**
 *_abs-function thta computes the absolute value of an integer.
 *@n: integer to check
 *Return:absolute value int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
